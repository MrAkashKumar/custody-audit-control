# PROD environment: externally supplied MySQL connection and schema required.
spring.config.import=classpath:application-mysql.properties
logging.level.com.custody=INFO
# Enable retention only after the environment's deletion policy has been approved.
cc.retention.enabled=${CC_RETENTION_ENABLED:false}
cc.retention.dry-run=${CC_RETENTION_DRY_RUN:true}
